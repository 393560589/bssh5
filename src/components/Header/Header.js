import React from 'react'
import style from './Header.less'

const Header = () => {
  return (
    	<div className={style.header}>
			<input type="text" />
    	</div>
  );
};

Header.propTypes = {
};

export default Header;
